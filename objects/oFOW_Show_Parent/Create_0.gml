// This is the parent object for the instance that 
// should clear the FOW effect. You can choose to use this directly
// by assigning it as the parent of another object, 
// or simply copy the script calls into the object you wish.

// Initialise some specific instance variables (see script for the arguments)

if(global.UseFOW)
	sFOW_InitInstance(8, x, y, true);

